spring:
  application:
    name: consumer
  kafka:
    bootstrap-servers:
      localhost:9094

  datasource:
    url: jdbc:postgresql://localhost:5432/payments_db
    username: payments
    password: DylanThomasFur1a

  jpa:
    hibernate:
      ddl-auto: update

stripe:
  api-key: ${STRIPE_SECRET_KEY}

server:
  port: 8085
