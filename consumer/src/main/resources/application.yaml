spring:
  application:
    name: consumer
  kafka:
    bootstrap-servers:
      localhost:9094

  datasource:
    url: jdbc:postgresql://localhost:5432/payments_db
    username: payments
    password: DylanThomasFur1a

  jpa:
    hibernate:
      ddl-auto: validate

stripe:
  api-key: ${STRIPE_SECRET_KEY}

consumer:
  processing-delay-ms: 9000

server:
  port: 8085
